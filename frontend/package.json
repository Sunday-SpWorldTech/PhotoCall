{
  "name": "photocall-frontend",
  "version": "4.0.0",
  "private": true,
  "description": "PhotoCall frontend for human-photo avatar WebRTC calling",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
